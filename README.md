# PostgreSQL Practice 1

This Python program connects to a PostgreSQL 18 database and runs the SQL command:
SELECT version();

## How to Run

1. Install the required library:
   pip install psycopg2-binary

2. Edit the file config.json with your database information.

3. Run the program:
   python app.py
